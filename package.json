{
  "name": "storybot-launcher",
  "version": "1.0.0",
  "private": true,
  "description": "Desktop launcher + website runner for Storybot",
  "scripts": {
    "setup": "npm install --prefix storybot-backend && npm install --prefix storybot-frontend",
    "dev:backend": "npm run dev --prefix storybot-backend",
    "dev:frontend": "npm run dev --prefix storybot-frontend",
    "dev": "concurrently -k -n BACKEND,FRONTEND -c green,blue \"npm run dev:backend\" \"npm run dev:frontend\"",
    "desktop": "node scripts/launch-storybot.mjs",
    "test:backend": "npm run smoke --prefix storybot-backend"
  },
  "dependencies": {
    "dotenv": "^17.2.0",
    "open": "^10.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
